# CREATE INDEX CONCURRENTLY cannot run inside a transaction block, so this migration is the first
# in the project to opt out of Flyway's default transactional execution.
#
# The tradeoff this buys and the one it costs:
#   Buys - the index builds do not hold a lock that blocks writes to users. A plain CREATE INDEX on
#          a table on the registration and login write paths is an availability event at any size.
#   Costs - a failed CONCURRENTLY build leaves an INVALID index behind rather than rolling back.
#          Every statement here is IF NOT EXISTS, so a re-run after a failure skips the ones that
#          completed; an INVALID index from a failed build must be dropped by hand before the retry.
#          Check with:
#            SELECT c.relname FROM pg_index i JOIN pg_class c ON c.oid = i.indexrelid
#             WHERE NOT i.indisvalid;
executeInTransaction=false
